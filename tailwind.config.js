/* 
  Tailwind CSS v4 uses CSS-first configuration.
  All theme overrides and utility extensions are configured via the `@theme` and `@utility` directives inside `src/index.css`.
  This config file is unused.
*/
